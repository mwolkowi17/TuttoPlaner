<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TuttoPlaner.MonthDetails"
             Title="{Binding MonthName}">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="daysList" ItemSelected="daysList_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell >
                            <Label Text="{Binding DayNumber}" TextColor="CadetBlue" Padding="15" FontAttributes="Bold"/>
                        </ViewCell>
                        <!--<TextCell Text="{Binding DayNumber}" TextColor="CadetBlue"/>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>